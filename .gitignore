/orthogonals
/vfiohost
# built beside the test: the binary refuses to run from a temp dir
/test/fault/orthogonals
# rapid writes a counterexample here on failure; reproduce, fix, then delete
testdata/rapid/
# tmt run directories and coverage merges land here
/dist/
# vendored only into the srpm tarball, never the repo
/vendor/
# rpmbuild --build-in-place byproducts
/debug*.list
/elfbins.list
*.test
coverage.out
coverage.html
*.prof
*.pprof

# editor / OS
*.swp
*.swo
*~
.DS_Store

# local agent settings
.claude/
