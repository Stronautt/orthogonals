# written by orthogonals: Looking Glass IVSHMEM file and the parent of the
# per-VM SPICE socket directories, both recreated each boot.
# SELinux labels come from the fcontext rules orthogonals adds.
f /dev/shm/looking-glass 0660 stronautt qemu -
# 0751: traversal only — listing would enumerate the managed VMs. The per-VM
# directories underneath carry the access gate; `vm define` writes them.
d /run/orthogonals 0751 root root -
